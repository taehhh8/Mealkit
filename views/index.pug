style.
    *{position: relative;}
    li{ list-style:none; padding: 0; display:inline; }
    li.mcate::after{ content:" > "; } li.cate1::after{ content:" > "; }
    a{text-decoration: none; }
    #contents{
        margin:0 25%;
    }

div
    include ./common/head
    
    
div#contents
    include ./common/temp/main
    // include #{page}
	
    
    
div
    include ./common/footer
	
	
script.
  //- alert("test")
  if (#{user}) {
    alert("환영합니다")
  }
